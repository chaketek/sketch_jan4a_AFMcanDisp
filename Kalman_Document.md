# Kalman_Document.md
## カルマンフィルタ設計ドキュメント

**作成日**: 2026年1月12日  
**対象信号**: 空気量 (raw_Ga)  
**目的**: 管内脈動ノイズのキャンセル

---

## 1. 概要

### 1.1 背景
エアフローメータ（AFM）の空気量信号は、吸気管内の脈動を拾ってしまうため、表示用途としては見づらい波形になります。この脈動ノイズをキャンセルしつつ、信号の応答性を維持するためにカルマンフィルタを適用します。

### 1.2 設計要件
| 項目 | 要件 |
|------|------|
| 対象信号 | raw_Ga（空気量）[g/s] |
| サンプルレート | 5ms (200Hz) |
| 許容遅延 | 数十ms以下 |
| 目的 | 脈動ノイズのスムージング |

---

## 2. カルマンフィルタとは

### 2.1 基本概念
カルマンフィルタは、ノイズを含む観測値から真の状態を確率論的に推定するアルゴリズムです。

**状態方程式（システムモデル）:**
$$x[k] = x[k-1] + w$$

**観測方程式:**
$$z[k] = x[k] + v$$

- $x[k]$: 真の状態（推定したい値）
- $z[k]$: 観測値（ノイズを含む測定値）
- $w$: プロセスノイズ（システムの不確かさ）〜 $N(0, Q)$
- $v$: 観測ノイズ（センサの不確かさ）〜 $N(0, R)$

### 2.2 アルゴリズム

**予測ステップ (Predict):**
```
x_pred = x[k-1]          // 状態予測（定常モデルなので変化なし）
P_pred = P[k-1] + Q      // 誤差共分散の予測
```

**更新ステップ (Update):**
```
K = P_pred / (P_pred + R)           // カルマンゲイン計算
x[k] = x_pred + K * (z[k] - x_pred) // 状態更新
P[k] = (1 - K) * P_pred             // 誤差共分散更新
```

### 2.3 パラメータの意味

| パラメータ | 意味 | 効果 |
|------------|------|------|
| Q（プロセスノイズ） | システムの変動しやすさ | 大きい → 応答速い、ノイズ残る |
| R（観測ノイズ） | センサのノイズ量 | 大きい → スムージング強い、遅延増える |

---

## 3. ローパスフィルタとの比較

### 3.1 基本的な違い

| 項目 | ローパスフィルタ (LPF) | カルマンフィルタ (KF) |
|------|------------------------|----------------------|
| 設計思想 | 周波数領域で高周波をカット | 確率論的に最適推定 |
| パラメータ | カットオフ周波数 | プロセスノイズ Q, 観測ノイズ R |
| ゲイン | 固定 | 動的に自動調整 |
| 理論的根拠 | 経験則 | 最小分散推定（数学的に最適） |

### 3.2 カルマンフィルタの優位点

#### ① 最適ゲインの自動調整
カルマンゲイン $K$ は毎ステップ計算され、状況に応じて自動調整されます。

$$K = \frac{P}{P + R}$$

- 推定が不確かな時（$P$大）→ ゲイン大 → 観測値を信用
- 推定が確かな時（$P$小）→ ゲイン小 → 予測値を信用

ローパスフィルタにはこの自己調整機能がありません。

#### ② 立ち上がり特性が良い

**1次LPF:**
```cpp
y[k] = α * x[k] + (1-α) * y[k-1]
```
初期値が0の場合、真値に収束するまで時定数分の遅れが発生します。

**カルマンフィルタ:**
- 初期時は $P$ が大きい → $K \approx 1$ → 最初から観測値を信用
- 収束後は $K$ が下がってスムージング効果を発揮

「立ち上がりが速くて、定常状態ではスムージング」という両立が可能です。

#### ③ 物理モデルを組み込める
カルマンフィルタはシステムモデルを設定できます。

例：
- 加速度センサから速度を推定（積分モデル）
- 位置と速度の2状態モデル
- 周期的な脈動モデル

ローパスフィルタにはこの柔軟性がありません。

#### ④ ノイズ特性の明示的な扱い
カルマンフィルタはノイズを確率分布（分散）として明示的に扱います。

- $Q$: プロセスノイズ（システムの不確かさ）
- $R$: 観測ノイズ（センサの不確かさ）

これにより理論的に最適な推定（最小分散推定）が得られます。

### 3.3 脈動キャンセル用途での比較

| 状況 | LPF | KF |
|------|-----|-----|
| エンジン始動時（急変） | 追従遅い | 素早く追従 |
| 定常運転（脈動除去） | 同等 | 同等 |
| 急加速・急減速 | 遅れる | 適応的に追従 |
| センサ異常値 | そのまま影響 | ある程度除去 |

### 3.4 数学的な等価性

定常状態のカルマンフィルタは1次LPFと等価になります。

**1次LPF:**
$$y[k] = \alpha \cdot x[k] + (1-\alpha) \cdot y[k-1]$$

**1次カルマンフィルタ（定常状態）:**
$$\hat{x}[k] = \hat{x}[k-1] + K \cdot (z[k] - \hat{x}[k-1])$$

定常状態では $K \to \frac{Q}{Q+R}$ に収束するため、$\alpha = K$ とすれば等価です。

ただし、過渡状態（立ち上がり）での挙動が異なることがポイントです。

---

## 4. フィルタ適用タイミング

### 4.1 結論
**物理量に変換した後にフィルタを適用する**のが適切です。

```
[CAN受信] → [デコード（物理量変換）] → [カルマンフィルタ] → [表示]
```

### 4.2 理由

#### ① ノイズ特性が物理量で定義しやすい
- 物理量 [g/s] → 「空気量のばらつきは ±5 g/s くらい」と直感的に設定可能
- 電圧 [V] → 「±0.02V」だとピンとこない

パラメータチューニングが楽になります。

#### ② 非線形変換がある場合の問題
センサ出力→物理量の変換が非線形の場合、電圧にフィルタをかけてから変換すると、ノイズ特性が歪みます。

物理量空間でフィルタリングすれば、この問題を回避できます。

#### ③ デバッグのしやすさ
フィルタ前後の値が物理量なので、値の妥当性を確認しやすくなります。

### 4.3 例外ケース

以下の場合は電圧にフィルタをかけた方が良いこともあります：

| ケース | 理由 |
|--------|------|
| ADC量子化ノイズが支配的 | 電圧レベルでスムージングした方が効果的 |
| 複数の物理量に同じ電圧を使う | 変換前にフィルタすれば1回で済む |
| 変換テーブル（非線形）が重い | フィルタ後に1回だけ変換で計算量削減 |

---

## 5. 実装仕様

### 5.1 ファイル構成
| ファイル | 役割 |
|----------|------|
| `kalman_filter.h` | カルマンフィルタの実装 |
| `sketch_jan4a_AFMcanDisp.ino` | フィルタの初期化と適用 |

### 5.2 データ構造

```cpp
typedef struct {
    float x;        // 推定状態（フィルタ出力）
    float P;        // 推定誤差共分散
    float Q;        // プロセスノイズ共分散
    float R;        // 観測ノイズ共分散
    bool initialized;
    uint32_t sampleCount;
} KalmanFilter_t;
```

### 5.3 プリセットパラメータ

| プリセット | Q | R | 遅延目安 | 用途 |
|------------|-----|-----|----------|------|
| `kalmanInitPulsation()` | 0.01 | 0.5 | 20-30ms | 脈動キャンセル（推奨） |
| `kalmanInitFast()` | 0.1 | 0.2 | 10-15ms | 高応答（ノイズ残りやすい） |
| `kalmanInitBalanced()` | 0.05 | 0.3 | 15-20ms | バランス型 |

### 5.4 使用方法

**初期化:**
```cpp
KalmanFilter_t rawGaFilter;
kalmanInitPulsation(&rawGaFilter);  // 脈動キャンセル用プリセット
```

**フィルタ適用:**
```cpp
float filtered_value = kalmanUpdate(&rawGaFilter, raw_measurement);
```

**パラメータ変更:**
```cpp
kalmanSetParams(&rawGaFilter, 0.03f, 0.4f);  // Q, R を個別指定
```

**リセット:**
```cpp
kalmanReset(&rawGaFilter);  // パラメータは維持、状態をリセット
```

---

## 6. パラメータ調整ガイド

### 6.1 調整の指針

| 症状 | 対処法 |
|------|--------|
| ノイズが残る | R を大きくする or Q を小さくする |
| 遅延が大きい | R を小さくする or Q を大きくする |
| 急変に追従しない | Q を大きくする |
| 異常値を拾う | R を大きくする |

### 6.2 推奨調整手順

1. まず `kalmanInitPulsation()` で動作確認
2. ノイズが気になる場合は R を増やす（0.5 → 0.8 → 1.0）
3. 遅延が気になる場合は Q を増やす（0.01 → 0.03 → 0.05）
4. バランスを見ながら微調整

### 6.3 遅延の目安

5ms サンプリングの場合：

| Q/R比 | 概算遅延 |
|-------|----------|
| 0.02 (Q=0.01, R=0.5) | 約25ms |
| 0.17 (Q=0.05, R=0.3) | 約15ms |
| 0.50 (Q=0.1, R=0.2) | 約10ms |

---

## 7. 参考文献

- Kalman, R.E. (1960). "A New Approach to Linear Filtering and Prediction Problems"
- Welch, G. & Bishop, G. "An Introduction to the Kalman Filter"
- ESP-IDF Programming Guide (Espressif Systems)

---

## 変更履歴

| 日付 | バージョン | 変更内容 |
|------|------------|----------|
| 2026/01/12 | 1.0 | 初版作成 |
